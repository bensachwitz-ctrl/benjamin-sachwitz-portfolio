## 2024-05-25 - Icon-only buttons lacking ARIA labels
**Learning:** Found multiple icon-only buttons (like modal close buttons, lightbox close buttons, hamburger menu) missing accessible names (aria-labels). This makes them opaque to screen reader users.
**Action:** Add aria-label attributes to icon-only interactive elements across the application to ensure screen reader compatibility.
