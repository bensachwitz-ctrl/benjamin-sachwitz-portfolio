## 2026-05-22 - Adding ARIA labels to Icon-Only Buttons
**Learning:** Icon-only interactive elements using `div` or generic elements create accessibility barriers. Replacing them with semantic `<button>` tags and `aria-label` descriptions significantly improves screen reader support.
**Action:** Always verify that buttons lacking visible text include an `aria-label` and manage states like `aria-expanded` for toggleable elements.
